name: Publish cicullis surface

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5

      - name: Prepare artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist
          mkdir -p dist/rules dist/boundary dist/actions
          cp CNAME dist/CNAME
          cp public/index.html dist/index.html
          cp public/rules/index.html dist/rules/index.html
          cp public/boundary/index.html dist/boundary/index.html
          cp public/actions/index.html dist/actions/index.html
          cp -R surface-system dist/surface-system
          test -f dist/index.html
          test -f dist/rules/index.html
          test -f dist/boundary/index.html
          test -f dist/actions/index.html
          test -f dist/surface-system/shell/base.css
          grep -Fx "cicullis.verifrax.net" dist/CNAME

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
